describe('Append words with and', () => {

   /*  it('should not append "and" between the words', () => {
        expect(appendAnd(100, 0)).toBe('and');
    });

    it('should append "and" between the words', () => {
        expect(appendAnd(100, 0)).toBe('and');
    });

    it('should append "and" before the words', () => {
        expect(appendAnd(100, 0)).toBe('and');
    });

    it('should append "and" after the words', () => {
        expect(appendAnd(100, 0)).toBe('and');
    }); */

});